export const environment = {
  production: true,
  apiBaseUrl: '/api',
  auth: {
    enabled: true,
    url: 'http://localhost:8081',
    realm: 'hippoject',
    clientId: 'hippoject-frontend'
  }
};
